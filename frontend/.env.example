# Client-facing API base URL used by axios in the React app.
# Default `/api` is recommended in dev because Vite proxies it to the backend.
VITE_API_BASE_URL=/api

# Backend origin used by Vite's dev proxy for `/api/*` requests.
VITE_BACKEND_URL=http://127.0.0.1:8002
